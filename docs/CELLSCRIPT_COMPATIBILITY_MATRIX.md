# CellScript Compatibility Matrix

**Snapshot date**: 2026-04-19
**Scope**: `/Users/arthur/RustroverProjects/Spora/cellscript/` and immediate CKB-style runtime integration assumptions  
**Purpose**: keep the implementation claims precise for four recurring questions:

1. Is CellScript fully compatible with CKB-VM?
2. Do `fn`, `action`, and `lock` have clear boundaries and work together cleanly?
3. How much real integration exists with Spora's DAG-oriented execution model?
4. Has CellScript already completed what the design documentation claims?

This document tracks **implementation reality**, not design intent.  
For broader implementation status, also see [CELLSCRIPT_IMPLEMENTATION_STATUS.md](./CELLSCRIPT_IMPLEMENTATION_STATUS.md).  
For proposal-vs-implementation coverage, see [CELLSCRIPT_DESIGN_IMPLEMENTATION_AUDIT.md](./CELLSCRIPT_DESIGN_IMPLEMENTATION_AUDIT.md).
For the original design target, see [SPORA_DSL_DESIGN_PROPOSAL_CN.md](./SPORA_DSL_DESIGN_PROPOSAL_CN.md).

## Executive Summary

CellScript currently has a real compiler path:

- source -> lexer/parser/type checker -> IR -> RISC-V assembly
- restricted pure-compute source -> generated RISC-V ELF
- restricted schema-parameter field access -> generated RISC-V ELF for fixed-width scalar fields (`bool/u8/u16/u32/u64`) through unaligned-safe little-endian byte loads
- restricted consumed-input field access -> generated CKB-runtime assembly lowering with `LOAD_CELL Source::Input` and loaded-byte bounds checks, while full consume semantics remain symbolic and ELF remains fail-closed
- restricted `read_ref<T>().scalar_field` access -> generated CKB-runtime RISC-V ELF with `LOAD_CELL Source::CellDep`, loaded-byte bounds checks, and byte-wise scalar loads
- restricted fixed-scalar `create Type { ... }` output verification in assembly with `LOAD_CELL Source::Output`, exact-size checks, bounds checks, and field equality checks
- restricted consumed-input-to-created-output `u64` field alias verification in assembly
- restricted left-associative `u64` add/sub chain verification from consumed input fields, constants, local constants, and entry parameters into created output fields, with simple move/alias propagation
- explicit fail-closed assembly paths for symbolic runtime features that do not yet have complete verifier lowering
- optional `vm-runner` feature -> no-argument pure ELF execution through `ckb-vm 0.24`
- compile metadata sidecar -> lowering/runtime/scheduler JSON, including explicit `fail_closed_runtime_features`
- compile metadata sidecar -> explicit VM object ABI declaration (`runtime.vm_abi.format = "molecule"`, `version = 0x8001`) for CKB-style full-object load syscalls; RISC-V ELF artifacts also embed a fixed ABI trailer
- compile result self-validation -> metadata schema version, compiler version, artifact bytes, BLAKE3 hash, metadata hash/size binding, ELF trailer, and standalone-runtime claims are checked before results are returned
- checked action effect declarations -> explicit under-declared `#[effect(...)]` annotations are rejected, including same-module indirect calls and local `path` dependency imports
- checked `fn` purity and call boundaries -> helper functions cannot hide direct, same-module indirect, locally imported Cell/runtime operations, `env::*` runtime builtins, or `type_hash()` Cell identity builtins; `fn` cannot call `action` or `lock`; no-return helper calls lower without a result destination and cannot be bound or returned as values
- checked return/value semantics -> value-returning `action` and `fn` must return on all paths; explicit returns, typed tail expressions, terminal `if` tail expressions, and block expressions whose tail statement is an `if` with `else` lower into real value paths
- checked dead-code semantics -> statements after guaranteed-return paths are rejected instead of being silently skipped by lowering
- checked assertion value semantics -> `assert_invariant` lowers to fail-closed verifier CFG and is typed as `Unit`, so it cannot be bound or used as a synthetic return value; messages must be static string literals
- checked local collection semantics -> empty arrays require explicit zero-length type annotations; `Vec.push` propagates item type from `Vec::new()` and rejects incompatible later pushes
- checked call lowering semantics -> unresolved calls no longer default to `u64`; lowering rejects unknown return types instead of fabricating callable values
- CKB-style `LOAD_CELL` ABI prelude in assembly for `consume`, `read_ref`, and `create` summaries

This still does **not** mean full CKB contract compatibility or full stateful runtime completion. The executable ELF path remains fail-closed for symbolic Cell/runtime features such as full `consume` / `create` resource-handle semantics, collections, transfer/claim/settle, lock/type script verification, and broader schema decoding beyond fixed-width scalar fields.

The most accurate short status line today is:

> CellScript targets Spora's CKB-VM-compatible execution substrate, has no-argument pure ELF runner support plus restricted parameterized schema ELF generation, emits auditable lowering/runtime metadata, and has partial CKB-style Cell runtime ABI lowering, but it is not yet a complete executable stateful contract language.

## Recommended Wording

Use:

- `CellScript compiles to RISC-V artifacts intended for Spora's CKB-VM-compatible runtime.`
- `CellScript ELF artifacts embed the Molecule VM object ABI (`0x8001`) for CKB-style full-object load syscalls; verifier loaders strip the trailer before CKB-VM execution and select the declared ABI.`
- `Pure-compute CellScript programs, plus restricted fixed-width schema parameter/read_ref field subsets, can be emitted as ELF or CKB-runtime ELF where no symbolic expression semantics remain.`
- `Consumed-input fixed-width fields are now concretely lowered in assembly, but consume expressions still keep ELF fail-closed until resource conservation semantics are implemented.`
- Narrow input-field-to-output-field equality, single-field `amount: u64` additive merge, and matched `amount` subtraction split checks exist for resource conservation, but generalized UDT/resource conservation is still incomplete.
- `Left-associative u64 add/sub chains over loaded fields, constants, local constants, and entry parameters can be checked, with simple move/alias propagation, but right-nested/arbitrary arithmetic state proofs are still incomplete.`
- `Read-only CKB-runtime ELF can use `LOAD_CELL Source::CellDep`, but it requires real transaction/syscall context and is not runnable through standalone `cellc run`.`
- `Mutating Cell/runtime programs currently emit assembly and metadata, but ELF remains fail-closed until resource/state-transition lowering is complete.`

Avoid:

- `CellScript is fully CKB compatible`
- `CellScript is complete`
- `CellScript has complete DAG integration`
- `CellScript already implements the full design proposal`

## Matrix

| Topic | Current Implementation Reality | Risk / Interpretation | Next Required Step |
|---|---|---|---|
| CKB-VM execution substrate | Real `riscv64-elf` generation exists for pure compute, restricted schema-parameter field loads, and restricted `read_ref` field loads through CKB `LOAD_CELL`. Consumed-input field loads now have concrete CKB-runtime assembly lowering, but `consume-expression` still makes ELF fail-closed. The optional `vm-runner` feature executes only no-argument pure ELF through `ckb-vm 0.24`. | This proves the substrate path, not the full stateful contract surface. Parameterized and CKB-runtime ELF need transaction/ABI/syscall context, not the standalone runner. | Add end-to-end execution through Spora's full verifier context with real transaction data. |
| Full CKB contract compatibility | Not complete. CellScript uses a CKB-style VM/syscall substrate but does not claim binary or semantic compatibility with arbitrary CKB contracts. | `Runs on CKB-VM` is true for the supported subset. `Full CKB contract compatibility` is false. | Keep substrate compatibility and contract compatibility separate in docs and release notes. |
| `riscv64-asm` target | Real and used by regression tests. Stateful features preserve symbolic operations, emit CKB-style `LOAD_CELL` ABI setup for access summaries, and fail closed for runtime features that lack complete verifier lowering. | Assembly is auditable, and unsupported verifier semantics are no longer silent success paths, but this is still not proof of executable state semantics. | Continue using assembly as transparent lowering output while building real schema decoding and invariant checks. |
| `riscv64-elf` target | Real for pure computation/control flow, fixed-width scalar field loads from named action/lock schema parameters, and fixed-width scalar fields loaded from `read_ref` CellDep bytes. Consumed Input field loads are concretely emitted in assembly, but `consume-expression` still rejects ELF. | Fail-closed behavior is correct; the executable target is intentionally a subset. Named schema parameter loads now use a pointer+length ABI, so fixed scalar field access can be bounds-checked. | Extend executable lowering feature by feature and keep rejection tests for unsupported operations. |
| CKB-style syscall ABI | Summary prelude uses `LOAD_CELL` with `A0=buffer`, `A1=size pointer`, `A2=offset`, `A3=index`, `A4=source`; Input access keeps `consume/transfer/destroy/claim/settle`, CellDep access keeps `read_ref`, and Output access keeps `create/transfer/claim/settle` provenance when a cell-backed output is produced. `transfer asset to addr`, `receipt Name -> Output` claim outputs, and `settle value` can map same-name same-type fixed-scalar fields from consumed inputs into output verifier checks; verifier-coverable transfer destination `Address` operands also bind to the transfer-created Output LockHash through `LOAD_CELL_BY_FIELD`. Named `destroy` operands now scan `GroupOutput` TypeHash fields with `LOAD_CELL_BY_FIELD`, treating `INDEX_OUT_OF_BOUND` as scan completion and `ITEM_MISSING` as an untyped output to skip. Runtime-required `claim-conditions` and `settle-finalization` obligation details now list field-aware consumed-input requirements such as `Input#0:receipt.amount=input-cell-field-u64[8]` and fixed-byte owner fields; explicit claim and vesting-style receipt claim flows also surface DAA/cliff/state/claimable source predicates plus checked witness-envelope and canonical ECDSA authorization-domain subconditions; native claims over receipts with a fixed `[u8; 20]` signer pubkey hash field convention additionally exact/bounds-check the consumed signer field, call `SECP256K1_VERIFY` syscall `3002`, and classify `claim-conditions:<Receipt>` as `checked-runtime` only when no source-level `assert_invariant` / DAA predicate is present, while signer-backed guarded claims remain top-level `runtime-required` with `source-predicate=runtime-required` plus `claim-source-predicate-gap`, and receipts without that explicit signer field still keep signature verification runtime-required; lifecycle-backed settle paths with a fixed-scalar `state` field exact/bounds-check consumed Input and created Output state bytes, require both to equal the final lifecycle state index, and classify restricted `settle-finalization:<T>` as `checked-runtime` when settle output admission is also covered. CellScript metadata declares Molecule VM object ABI `0x8001` for full-object `LOAD_SCRIPT` / `LOAD_INPUT` / `LOAD_CELL` / `LOAD_HEADER`; RISC-V ELF artifacts embed a fixed ABI trailer; the exec verifier strips it and selects `VmAbiFormat::Molecule`. Consumed-input field reads, `read_ref` field reads, simple `create` output fields, simple input-field-to-output-field equality checks, and left-associative `u64` add/sub expected expressions now perform loaded-byte bounds checks. | ABI shape is now aligned for these paths, but assembly/non-ELF artifacts still rely on sidecar/verifier policy. Full payload decoding, generalized transfer semantics beyond checked output field/lock rebinding, broader burn/conservation policy, generalized claim signer policy beyond the explicit 20-byte field convention, non-lifecycle/generalized settle finalization, and complete semantic validation are still incomplete. | Add authenticated manifests for non-ELF artifacts, then add generalized typed schema decoding and compare source-level invariants against loaded cell bytes. |
| Typed schema layout | IR and metadata now expose field offsets and fixed encoded sizes, and schema definitions reject duplicate field names plus `_` wildcard field names before layout maps are built. Schema and enum payload storage types also reject `&T`, `&mut T`, and `read_ref T`, keeping references as execution-scope values rather than persistent serialized fields. Codegen can lower `param.scalar_field` for named schema parameters, consumed `input.scalar_field`, and `read_ref<T>().scalar_field` into unaligned-safe byte-wise loads for `bool/u8/u16/u32/u64`. Schema parameters, consumed-input, and `read_ref` fields include exact-size and bounds checks when their source supplies a length word. Named schema parameter `type_hash()` now uses an explicit trusted 32-byte pointer+length ABI pair and exposes that requirement in parameter metadata. Local fixed-array literals now parse, type-check homogeneous elements, require explicit zero-length annotations for empty arrays, reject immutable element writes, lower compile-time-constant element reads/writes, unroll local foreach, and fold local `len()` calls without symbolic runtime fallback. Local tuple literals lower static field projection/assignment/destructuring without symbolic field access, and array-of-tuples static index projection plus local foreach destructuring preserve tuple metadata. `Vec.push` propagates item types from `Vec::new()` and rejects incompatible pushes once the vector has a concrete item type. Simple fixed-scalar `create` outputs now require all fields to be verifier-covered, enforce exact schema size, and check field equality against constants, parameters, consumed/read schema field aliases, or already-computed scalar locals; fixed-byte output fields can also be verified against trusted schema-parameter TypeHash bytes and created Output TypeHash bytes. `u64` fields additionally support local constants or left-associative `u64` add/sub chains over prelude-available operands; simple move/alias propagation is supported. Incomplete `create` verifier summaries fail closed at runtime instead of continuing after a warning comment. Direct one-external-resource-Input to one same-type-created-Output field aliases, restricted same-type resource merges where every consumed Input contributes exactly one single-field `amount: u64` leaf to one created Output's verifier-recomputed `Add` expression, and restricted one-Input amount splits where the remaining Output is `input.amount - split_terms` and every split term is matched by a sibling created `amount` Output are classified as `resource-conservation:<T>` `checked-runtime` and surfaced as checked `resource-conservation-proof` transaction inputs; unmatched or duplicate split outputs, extra fields, or generalized conservation are still `runtime-required` and policy-visible as `resource-conservation-proof-gap`. | This is not full schema decoding, but schema identity no longer silently changes through duplicate field map overwrite or reference-typed fields that cannot be serialized with a stable lifetime. Full consume/create expression/resource-handle semantics remain fail-closed, generalized resource conservation is not complete, dynamic array indexes are not fully executable, and nested/dynamic cell fields are not handled. | Add nested/dynamic decoding and verify source-level invariants against loaded cell bytes. |
| Field initializer checking | `create` and struct literals now validate against declared schema fields: duplicate fields, unknown fields, missing fields, and field type mismatches are compile errors. `create` additionally requires a cell-backed `resource`, `shared`, or `receipt` target, so ordinary `struct` values cannot masquerade as transaction outputs. Lifecycle receipt `state` omissions still flow to the lifecycle checker so the diagnostic remains protocol-specific. Empty `Vec::new()` is accepted only as contextual initialization for a concrete `Vec<T>` field. | This removes a major false-safety edge where source could construct partially specified or misspelled cells while metadata still looked schema-backed. | Extend contextual typing beyond empty vectors only after each literal/lowering rule is specified. |
| `read_ref` target checking | `read_ref<T>()` now requires `T` to resolve to a cell-backed `resource`, `shared`, or `receipt` type. Ordinary `struct` schemas cannot enter CellDep access metadata or CKB runtime lowering through `read_ref`. | This prevents a source-level memory/schema type from being audited as a transaction dependency read. | Add richer dependency selectors and authenticated OutPoint/type-id binding rules. |
| Stateful operand ownership | `consume`, `transfer`, `destroy`, `claim`, and `settle` now require named cell-backed linear operands. Non-cell structs/scalars and anonymous expressions are rejected before IR lowering. `let` bindings over existing linear values now move the source binding before introducing the alias, so `let copied = token` cannot leave both `copied` and `token` available. Local `let` bindings cannot store read-only references rooted in linear Cell values, so `let view = &token` and `let field = &token.amount` cannot leave a non-linear alias alive across a later `consume` / `transfer` / `destroy`; direct helper-call borrowing such as `helper(&token)` remains the supported short-lived form. User-visible local bindings cannot reuse an existing visible name, including duplicate tuple destructuring names and block/loop/branch shadowing. Field/index assignments must also be rooted at a named local or parameter, so temporary call results and temporary `read_ref<T>()` values cannot become mutation targets that bypass mutability and ownership tracking. Read-only references remain non-writable even when the reference binding itself is declared `mut`; only `&mut T` or mutable owned non-linear local values may be field/index assignment roots. Leading `mut` on callable parameters cannot mark cell-backed values, read_ref parameters, read-only references, or redundant `&mut` references as mutable state parameters. | This closes a gap where source could appear to consume or transfer a resource while the compiler had no local ownership binding to update, another gap where a linear value could be copied or kept through local aliasing, and further gaps where type-state or IR variable maps could silently overwrite a prior binding, mutate an untracked temporary, write through a read-only reference, or classify owned/read-only Cell ABI parameters as mutable state. | Add richer move/path ownership only if the linear type system can prove equivalent ownership for projections or destructured resources. |
| Branch/block/match/loop linear-state handling | `if` statements, `if` expressions, and `match` expressions now conservatively merge linear ownership state. Tuple/array aggregates containing linear values are themselves linear, wildcard bindings cannot discard linear values, and field/index projection such as `pair.0` or `items[0]` cannot move a linear element out of an aggregate until partial-move semantics are specified. If all continuing, terminal, tail-return, expression, block-tail-if, or match-arm branches agree, the merged state is kept; if a no-else or mismatched branch consumes/transfers/destroys/returns/moves a resource only on some paths, compilation fails. Explicit `return token` inside both branches, tail-if `if flag { token } else { token }`, `let moved = if flag { token } else { token }`, `let moved = { if flag { token } else { token } }`, and `let moved = match flag { Flag::On => token, Flag::Off => token }` are accepted because all paths move the same resource. Block expressions also propagate parent-visible linear state out of their child scope while keeping block-local bindings scoped, so `{ destroy token }`, `{ token }`, `{ let inner = token; inner }`, and `{ let inner = token; if flag { inner } else { inner } }` participate in the same ownership model; unhandled block-local resources such as `{ let out = create Token { ... }; 1 }` are rejected. Loop bodies are intentionally stricter: they must complete loop-local linear bindings and may not change parent-visible resource ownership state, because a `for` / `while` body can execute zero or multiple times. | This prevents branch-local, match-arm-local, block-local, loop-local, aggregate-held, wildcard-discarded, or field/index-projected resource consumption/returns/moves from disappearing when control returns to the parent scope, when all branches terminate, when an `if` statement is the typed tail return expression, when an `if` expression or `match` expression feeds a linear binding, when a block expression wraps the move/stateful operation, when a block's final statement is a value-producing `if`, when a loop body would otherwise consume a copied child-scope parent resource and then allow reuse after the loop, when `_` would otherwise discard a resource, or when aggregate projection would otherwise duplicate the parent aggregate's ownership. | Extend the same path-sensitive ownership merge only to control-flow forms with precise execution-count semantics; keep loops and aggregate projection conservative until linear iteration and partial-move semantics are specified. |
| Enum / match lowering | Field-less enum variants lower to discriminants. Unknown enum variant values are rejected, and payload enum variants cannot be used as bare values until payload construction lowering exists. Enum `match` now type-checks unknown variants, duplicate arms, non-exhaustive arms without `_`, and rejects payload-variant patterns until payload destructuring lowering exists. Exhaustive enum matches without wildcard lower with an invalid-discriminant fail-closed branch. | This closes a previous gap where source could look exhaustively audited while IR required a hidden wildcard fallback or failed later in lowering. Payload enum types can still be declared for schemas, but payload construction/destructuring is intentionally blocked where lowering would be unsound. | Implement typed payload enum construction, decoding, destructuring, and source-map-visible lowering before allowing payload variant values or payload match patterns. |
| Protocol semantic lowering | IR records operation-tagged `consume_set`, operation-tagged `read_refs`, operation-tagged `create_set`, and mutable Cell `mutate_set` replacement ABI / field summaries; generated metadata includes runtime access summaries and scheduler witness hex with operation/source/index/binding-hash access records, including `mutate-input` and `mutate-output`. Mutable replacement TypeHash/LockHash preservation has executable `LOAD_CELL_BY_FIELD` checks and `checked-runtime` metadata status. Fixed-width preserved fields that fit the verifier scratch buffer now have executable Input/Output byte equality checks and `checked-runtime` field equality status. Simple scalar mutable transitions of the form `field = field +/- operand` are captured when the operand is verifier-coverable, so `MintAuthority.minted = old + amount` is checked at runtime and AMM Pool add/sub/mul/div/min computed-local deltas now report `checked-runtime` for the controlled formulas; non-covered mutable transition/equality formulas now emit `mutate-field-transition` / `mutate-field-equality` runtime input requirements with `state-transition-formula-gap` / `state-field-equality-gap`. Transfer-created outputs now carry verifier-coverable destination lock operands, so Output LockHash rebinding and destination-address binding are `checked-runtime` for the supported `Address` path. Destroy-created absence is now checked for named cell-backed operands by scanning `GroupOutput` TypeHash fields and classifying `destroy-output-absence` / `destroy-group-boundary` as `checked-runtime` when the consume pattern has a known TypeHash. Claim/settle output relations are classified as `checked-runtime` when fields are verifier-covered and now also emit status-classified `claim-output-relation` / `settle-output-relation` transaction runtime input components with `claim-output-relation-gap` / `settle-output-relation-gap` for unsupported output shapes, while runtime-required claim condition and settle finalization obligations expose field-aware consumed-input requirements in their details; claim paths now also check witness format and authorization-domain sighash, vesting-style `claim_vested` reports checked DAA/cliff/state/claimable/witness-format/authorization-domain subconditions inside the still-runtime-required `claim-conditions:VestingGrant` obligation, receipts with an explicit fixed 20-byte signer pubkey hash field convention call `SECP256K1_VERIFY`, classify `claim-witness-signature` as `checked-runtime`, and classify native `claim-conditions:<Receipt>` as `checked-runtime` only when no source-level `assert_invariant` / DAA predicate is required; signer-backed guarded claims stay top-level `runtime-required` with `source-predicate=runtime-required` while retaining checked signature/key-binding subconditions and now emit `claim-source-predicate` transaction runtime inputs with `claim-source-predicate-gap`. Lifecycle-backed fixed-scalar settle final-state plus settle output admission classify restricted `settle-finalization:<T>` as `checked-runtime`. Checked restricted resource conservation and runtime-required generalized resource conservation both emit `resource-conservation-proof` transaction runtime input requirements, with `resource-conservation-proof-gap` only on the unresolved shapes. Pool creation, mutation-invariant, and composition gaps are separately exposed as runtime-required `pool-pattern` obligations and structured schema v20 `pool_primitives[]` records with checked/runtime components, named `invariant_families[]`, source invariant counts, ABI indices, bindings, callee/source data, field-aware runtime input requirements, field summaries, blocker strings, and stable blocker classes; seed Pool token-pair symbol admission, positive-reserve admission, and fee-policy admission are checked only when the source guards and create-output fields are both verifier-covered, LP supply is checked only when `Pool.total_lp` and `LPReceipt.lp_amount` share the same verifier-covered source, controlled token-pair asset identity/type-id inequality admission is `checked-runtime` by loading Input `token_a` / `token_b` TypeHash fields with `LOAD_CELL_BY_FIELD` and rejecting equal 32-byte values, controlled launch/pool `pool-id-continuity` is `checked-runtime` through callee output-field coupling plus the tuple return-register ABI, and controlled `swap_a_for_b` LP supply consistency is `checked-runtime` through preserved `Pool.total_lp` equality; broader generalized Pool identity/admission policy remains incomplete but is pinned with `phase2-deferred-pool-admission`; `swap_a_for_b` fee accounting and constant-product pricing remain runtime-required but now expose field-level ABI sources for `input.amount`, `pool.fee_rate_bps`, and Pool reserve Input/Output fields plus `phase2-deferred-pool-fee-policy` / `phase2-deferred-amm-pricing` blocker classes; `add_liquidity` proportional-liquidity accounting, `remove_liquidity` proportional-withdrawal / LP supply consistency, and AMM mutation reserve-conservation and pool-specific-admission families also remain runtime-required while exposing token/receipt amount, token symbols, Pool reserve/total_lp, Pool token symbols, Pool type_hash, created LP receipt, and created token amount/symbol field ABI requirements with stable Phase-2-deferred classes; launch composition additionally exposes callee Pool admission and launch-pool atomicity as runtime requirements over `Param` / `Output` ABI sources, marks the verifier-covered launch atomicity subcomponents for minted amount, seed amount, symbol consistency, and fixed tuple-array distribution sum plus pool seed bounded by initial mint as checked components, marks callee-admission argument handoff for seed token symbol, paired token symbol, and fee bound as checked components, and marks controlled pool-id continuity tuple-return projection, Pool type_hash / LPReceipt.pool_id return ABI handoff, and callee output-field equality as checked components. CLI JSON, deny-policy diagnostics, docgen Markdown/HTML, and docgen JSON now include flattened Pool runtime input requirement counts/summaries, runtime-required Pool invariant blocker-class summaries, non-Pool transaction-invariant checked-subcondition summaries, and status-classified transaction runtime input requirement summaries with runtime-required blocker reasons, blocker classes, and dedicated blocker/class summaries. Generated assembly rejects unsupported symbolic runtime operations instead of leaving them as successful silent paths. | Access summaries, mutation ABI summaries, checked identity preservation, checked preserved-field equality, checked transfer output lock rebinding for supported destinations, checked destroy grouped-output absence scans, checked claim/settle output relation components and blockers, checked claim witness envelope/domain separation and explicit signer-field ECDSA verification, checked signer-field native claim-condition classification guarded by source-predicate detection, claim-source-predicate blocker visibility for signer-backed guarded claims, checked lifecycle settle finalization for the restricted final-state/output-admission path, checked resource-conservation-proof components for restricted conservation, parameter/parameter-field/computed-local transition checks, explicit claim/settle consumed-input details, status-classified transfer/destroy/claim/settle/resource-conservation/mutable-state transaction runtime input requirements with blocker strings/classes and CLI blocker summaries for remaining runtime-required components, checked vesting claim source predicates, explicit structured pool-pattern debt with named invariant families, field-aware runtime input requirements, blocker strings/classes, partial pool-pattern admission/atomicity/ABI-handoff closure, checked controlled pool-id continuity and swap LP-supply relation metadata, composition ABI surfacing, reporting/policy visibility, and fail-closed paths are real, but not yet equivalent to complete pool-pattern semantics or a generalized verified state-transition proof for arbitrary formulas. | Connect access summaries, mutation summaries, `pool_primitives[].invariant_families[]`, non-Pool transaction-invariant checked subconditions, and Pool field-aware runtime input requirements to richer executable transition-formula verifier code and scheduler admission checks. |
| `fn` semantics | `fn` is now a distinct AST/IR/metadata category (`FnDef` -> `IrPureFn` -> `functions[]`). It is enforced as pure: directly inferred, same-module call-derived, and local `path` dependency import-derived `ReadOnly`, `Creating`, `Destroying`, or `Mutating` behavior is rejected. A `fn` cannot call an `action`, a `lock`, `env::*` runtime builtins, or `type_hash()` Cell identity builtins. Helpers without a return type use internal `Unit`, lower to `Call { dest: None }`, and cannot be bound or returned as values. | Pure helper behavior is no longer represented as a scheduler-visible action, stateful/runtime behavior can no longer hide behind local `fn` call chains, imported local package calls, env runtime builtins, or Cell identity builtins, and no-return helpers no longer masquerade as `u64` values in IR. Remote registry dependency summaries still need a stable signed format. | Extend this through registry package summaries and richer purity/effect manifests. |
| Callable signature checking | Callable definitions now reject duplicate parameter names, visible binding/constant shadowing, and `_` wildcard parameters across `action`, `fn`, and `lock`, so ABI/metadata parameter identity remains stable. Callable return types cannot contain references; `&T` and `&mut T` remain parameter/short-lived expression forms only until a real lifetime model exists. Local, same-module qualified, and local path imported function/action/lock calls validate argument count and argument types before lowering. Read-only reference parameters accept mutable references (`&mut T` -> `&T`), but not the reverse. Unknown namespaced constructors such as `Missing::new()` are rejected instead of fabricating a `Named(Missing)` value. Builtins and methods now enforce arity/type rules for `env::current_daa_score`, `Address::zero`, `Hash::zero`, `Vec::new`, `min/max/isqrt`, `len`, `type_hash`, `push`, and `extend_from_slice`. | This closes prior semantic holes where parameter names could be overwritten before metadata/IR lowering, where a call could resolve only by return type while wrong arguments still reached IR/codegen, or where references could escape callable bodies without lifetime tracking. Registry package signature summaries remain unsigned/incomplete. | Extend the same checks through signed registry summaries and richer std/template-generated signatures. |
| Consensus failure handling | Hidden failure helpers `unwrap`, `expect`, and `unwrap_or` are rejected during type checking for direct, method-style, and namespaced calls. | This prevents source that looks deterministic from hiding implicit failure control flow. Full `Result` / `Option` and `?` semantics are still not implemented. | Add explicit `Result` / `Option` ADTs, match exhaustiveness, and transparent `?` lowering once the error model is specified. |
| Named type resolution | User-declared schema fields, callable signatures, and explicit local type annotations must resolve to known local/imported types or supported internal builtins. `Option` and `Result` are reserved and rejected until their explicit error semantics exist. | This blocks a previous false-safety edge where unknown type names could enter IR and metadata as if they were valid schema objects. | Keep adding standard-library types only when their lowering and verifier ABI are specified. |
| Symbol resolution uniqueness | Duplicate top-level symbols in a module and collisions between local symbols and import aliases are rejected. Duplicate module names across source roots are also rejected. | This prevents `HashMap` overwrite behavior from changing what auditors think they are reviewing. | Add signed package summaries for remote registry dependencies so symbol uniqueness and effect summaries remain stable across distribution. |
| `action` semantics | Real state-transition entry abstraction. Effects, scheduler hints, access summaries, and metadata are emitted. Value-returning actions must return on all paths via explicit `return`, typed tail expressions, or terminal `if` branches with typed tail expressions, and lowering emits real return terminators for those forms. Statements after guaranteed-return paths are rejected. `assert_invariant` is value-less `Unit`, requires a static string literal message, and cannot satisfy a value return. Calls whose return type cannot be resolved are rejected instead of lowered as `u64`. Capability gates now reject `transfer` without `transfer`, `destroy` without `destroy`, `claim` on non-receipts, invalid receipt claim outputs, and `settle` on non-cell values. | The surface is meaningful, but resource/effect correctness is not yet fully enforced as a theorem. | Add compiler checks proving declared effects match all lowered stateful operations and complete resource conservation checks. |
| `lock` semantics | Has a distinct pipeline and non-bool lock return definitions are rejected. Claim lowering now checks the basic witness envelope and canonical ECDSA authorization-domain sighash for covered claim paths, but lock-level signer binding still needs hardening. | Stronger than before, but signer-key binding and replay-resistant witness policy still need protocol-level specs. | Add explicit signature/witness binding specs and negative tests for replay/domain mistakes. |
| `fn` / `action` / `lock` cooperation | `fn` is pure helper code, `action` is state-transition entry code, and `lock` is authorization predicate code. `action` and `lock` may call `fn`; `fn` may call only `fn` and cannot call `env::*` runtime builtins or `type_hash()` Cell identity builtins; `lock` cannot call `action` or another `lock`. Codegen emits `action`/`lock` entrypoints before helper functions so helpers are not accidentally selected as VM entrypoints. | Boundaries are usable and tested, but signer-key binding and witness policy for `lock` still need stronger protocol-level specs. | Document exact witness binding and authorization helper rules; extend call-boundary checks across signed registry packages. |
| Effect and capability classes | Effect classes are inferred from `read_ref`, `consume`, `create`, `destroy`, `transfer`, `claim`, `settle`, same-module calls, and local `path` dependency imports; explicit under-declarations are rejected. Capability declarations are now semantically checked for the core stateful operations and attribute/inline capability syntax is merged. | This blocks low-reported scheduler metadata and several false resource-authority claims for local sources, but it is still not a complete static resource proof. | Extend effect checking through signed registry summaries and future resource lifecycle rules. |
| `touches_shared` | Metadata now exposes non-empty shared-touch summaries when stateful shared reads are present. | Better scheduler visibility, but derivation still needs a formal spec and verifier consumer. | Specify hash/domain derivation and wire it into the real scheduler path. |
| Access summary metadata | `metadata_schema_version`, `compiler_version`, `artifact_hash_blake3`, `artifact_size_bytes`, `source_hash_blake3`, `source_content_hash_blake3`, `source_units[]`, `types[].type_id`, `types[].type_id_hash_blake3`, `types[].capabilities`, `types[].claim_output`, `params`, `functions[]`, operation-tagged `consume_set`, operation-tagged `read_refs`, operation-tagged `create_set`, `mutate_set`, `pool_primitives[]`, `ckb_runtime_accesses`, `ckb_runtime_features`, `runtime.vm_abi`, `standalone_runner_compatible`, `symbolic_runtime_features`, `fail_closed_runtime_features`, `verifier_obligations`, `transaction_runtime_input_requirements`, and `elf_compatible` are emitted as JSON. | CI/audit tooling can distinguish stable declared type identity, pure helpers, type capability authority, receipt-to-output type promises, checked-static resource-operation gates, verifier-covered `checked-runtime` lifecycle and claim/settle output obligations, unresolved runtime-required transaction invariants, checked transfer output-relation / destination lock / destination address components, `transfer-output-relation-gap` blocker classes for uncovered transfer relations, checked/runtime `claim-output-relation` and `settle-output-relation` components with `claim-output-relation-gap` / `settle-output-relation-gap`, checked destroy grouped-output absence/group-boundary inputs, checked source-predicate subconditions inside vesting claim obligations, `claim-source-predicate-gap` blocker classes for signer-backed guarded claim source predicates, status-classified witness/time/finalization runtime input requirements, including checked claim-time and settle output-admission sub-inputs plus blocker strings/classes for remaining runtime-required components, mutable replacement TypeHash/LockHash preservation that is now `checked-runtime`, mutable replacement preserved-field equality that is `checked-runtime` for the covered fixed-width subset, `checked-runtime` or `checked-partial` mutable transition status, mutable shared-state transition-field obligations with replacement Input/Output ABI and field summaries, `state-transition-formula-gap` / `state-field-equality-gap` blocker classes for uncovered mutable state components, named Pool invariant families plus field-aware runtime input requirements with blocker strings/classes, unresolved mutable resource/receipt cell-state transition-field obligations where formulas are not covered, source provenance, path-bound source identity, path-independent source content identity, CKB-runtime ELF, standalone runner ELF, required VM object ABI, symbolic requirements, explicit fail-closed runtime paths including incomplete output verifier coverage, lowering obligations, whether an Input access came from `consume`, `transfer`, `destroy`, `claim`, `settle`, or `mutate-input`, and whether a cell-backed Output access came from `create`, `transfer`, `claim`, `settle`, or `mutate-output`. Scheduler witness version 1 also carries operation/source/index/binding-hash records so a DAG scheduler can consume compact Borsh metadata without depending solely on JSON; schema v20 keeps that compact witness limited to scheduler-visible Input/CellDep/Output cell-state accesses and omits runtime-only witness/signature syscalls. Compile results now self-validate schema/toolchain compatibility plus artifact/source/metadata binding; CLI policy now consumes runtime-required Pool invariant families and their sources/blocker classes, and CLI/docgen separately expose runtime-required transaction-invariant checked subconditions and status-classified transaction runtime input requirements plus dedicated runtime-required blocker/class summaries; consensus/runtime policy still has to enforce them. | Add CI policies and runtime checks that reject inconsistent metadata/artifacts. |
| DAG integration | Scheduler metadata and Borsh witness bytes are emitted; witness version 1 includes effect class, shared-touch hashes, estimated cycles, and operation-tagged access records. `ActionMetadata::scheduler_witness_bytes()` exposes compiled metadata bytes, and `spora-exec` CellTx now has low-level helpers to attach/discover CellScript scheduler witnesses in ordinary transaction witnesses by `0xCE11` magic/version, decode/admit the Borsh envelope with magic/version/count checks, effect/operation/source id checks, operation/source compatibility checks, concrete transaction Input/CellDep/Output index bounds checks, exact operation/source/index/binding_hash multiset comparison against trusted transaction-builder or compiled-metadata summaries, and produce a trusted access summary while appending compiled metadata bytes to a concrete transaction. Consensus MPE `BlockAccessSummary` now consumes admitted witnesses, can require trusted builder/metadata access multisets before merge, merges scheduler-visible Input/CellDep/Output accesses into structural summaries, and uses `touches_shared` as read/write contention domains for execution-DAG serialization. Mempool validation and template prefiltering now reject malformed CellScript scheduler metadata. Mempool entries, candidate snapshots, and mining selectors can carry producer-backed trusted summaries into `TemplateTransactionSelector::selected_cellscript_scheduler_accesses()`, including trusted empty summaries, so the strict template policy path can consume them. Wallet transaction generation can attach one compiled scheduler witness to the final transaction and expose the returned trusted summary on `PendingTransaction`; focused mining coverage proves a producer-returned summary survives sidecar insertion into selector exposure; focused consensus coverage proves selector-provided builder summaries are accepted/rejected by strict template prefiltering. | This closes the Phase 3 operational DAG integration path. Remaining work is Phase 4 hardening: RPC trusted-summary submission needs an explicit trust/authentication policy, and broader adversarial/property coverage is still needed before production trust. | Add authenticated external submission policy only if RPC-side builders must participate in strict template policy; otherwise keep trusted summaries on in-process builder/mining APIs and harden tests. |
| CLI ecosystem | `build`, `check`, `doc`, `fmt`, `init`, `add`, `remove`, `clean`, `info`, `metadata`, `verify-artifact`, package compilation, compile-test discovery, and feature-gated `run` have real paths. `build` and `check` now have CLI and manifest `[policy]` metadata gates for production, fail-closed, symbolic runtime, CKB runtime, runtime-required verifier obligations, and runtime-required Pool invariant families; `build` applies the gate before writing artifacts and `build --json` emits a machine-readable artifact/metadata/hash summary with fail-closed/runtime-required obligation counts plus checked/runtime-required Pool invariant-family counts; `check --all-targets` validates both asm and ELF lowering without writing artifacts, and `check --json` emits a machine-readable checked-target/policy summary with the same counts. `init --json`, `add --json`, `remove --json`, `clean --json`, and `info --json` cover local package lifecycle summaries for CI/IDE callers; `add/remove --dev/--build` now target the correct manifest sections and `add --git/--path` writes detailed dependency sources. `verify-artifact` validates artifact/metadata binding for CI without recompilation, `--verify-sources` checks source-unit hashes against files on disk, `--expect-*hash` pins artifact/source/source-content BLAKE3 values, `--production` / `--deny-*` applies the same metadata policy gates to stored artifacts, including runtime-obligation and Pool invariant-family rejection, and `--json` emits a machine-readable summary with obligation/family counts. `doc` emits API docs plus lowering audit reports and verifier obligations. Registry/package network commands remain fail-closed. | Core local developer workflow is usable; ecosystem distribution is not complete. | Finish registry install/publish/update and expand CI policy presets. |
| Docgen/fmt/package/test/wasm | `doc` and `fmt` are real enough for package sources. `doc --json` reports generated output metadata, and `fmt --json` reports clean/dirty changed-file summaries. Package discovery and path dependencies exist. `cellc test` discovers `tests/**/*.cell` and supports positive compile tests, strict `// cellscript-test: expect-success` / `expect-fail` / `expect-error` diagnostics, per-file target selection, per-file production/symbolic/CKB/runtime-obligation policy gates, runtime metadata assertions, verifier-obligation and runtime-required-obligation assertions, action/function/lock metadata classification assertions, and `--json` CI summaries. Unknown or conflicting test directives fail. `src/wasm/` is now compiled and tested, but explicitly fail-closed for executable action/lock lowering. | Tooling has real local paths, but not yet Solidity/Move-grade. | Implement real property/invariant/fuzz testing and either build a real Wasm backend or keep it explicitly unsupported. |
| IDE/LSP | VS Code extension and in-crate LSP path exist with diagnostics/formatting/navigation support. Action hover, receipt hover, diagnostics, and code actions now surface lowering metadata, ELF compatibility, symbolic runtime features, verifier obligations, lifecycle states/transitions, and CKB access summaries. | Useful but not yet a mature language server with semantic indexing, full build graph awareness, and rename safety. | Add semantic tokens, cross-package indexing hardening, safer rename, and compiler-version synchronization. |
| Design proposal completion | The design proposal remains ahead of code. Current code is a working compiler/toolchain under production hardening with metadata paths, effect checks, lifecycle state/transition metadata, static lifecycle guards, LSP lifecycle diagnostics, fixed-scalar lifecycle transition prelude checks and claim/settle output relation checks reported as `checked-runtime` when the fixed-field verifier is complete, executable mutable replacement TypeHash/LockHash preservation, fixed-width preserved-field equality checks, scalar mutable transition checks for parameter, parameter-field, and add/sub/mul/div/min computed-local operands, explicit shared-state and mutable cell-state transition-field obligations with `mutate_set` replacement ABI and field summaries, local fixed-array static index/foreach/len lowering, and local tuple destructuring / array-of-tuples static projection lowering for complete output verifier paths. | Treat the proposal as target architecture, not shipped truth. | Keep this matrix and implementation-status docs updated after each semantic milestone. |

## Direct Answers

### 1. Is CellScript fully compatible with CKB-VM?

No, if “compatible” means complete CKB contract semantic compatibility.

Yes, only in the narrower substrate sense:

- pure CellScript ELF can target CKB-VM-compatible RISC-V
- named schema parameters with fixed scalar field access can compile to ELF using a pointer+length Borsh blob ABI and runtime bounds checks
- consumed, transferred, claimed, settled, and destroyed input fixed scalar fields can be represented in operation-tagged `consume_set` and loaded from `LOAD_CELL Source::Input` bytes with bounds checks when the field path is otherwise supported
- transfer-created, claim-created, and settle-created outputs can verify same-name same-type fixed-scalar output fields against the consumed input cell
- created output fixed `u64` fields can be compared against consumed input fixed `u64` field aliases
- created output fixed `u64` fields can be compared against left-associative `+/-` chains over consumed fields, constants, local constants, and entry parameters
- `read_ref<T>().scalar_field` can compile to CKB-runtime ELF using `LOAD_CELL Source::CellDep` plus bounds checks
- simple `create` output fields can be checked in assembly against constant/parameter `u64` expected values
- `cellc run` can execute no-argument pure ELF through `ckb-vm 0.24` when built with `vm-runner`
- stateful assembly lowering now uses CKB-style `LOAD_CELL` register conventions
- full-object syscall output can be selected as Molecule ABI `0x8001` from the embedded ELF ABI trailer or by passing the artifact metadata ABI into the exec verifier

The current hard boundary is:

> Pure compute, narrow schema-parameter field access, and narrow read-only `read_ref` field access are executable. Narrow consumed-input field access is concretely auditable in assembly, but broader mutating Cell/runtime semantics are not yet fully executable as resource-conserving state transitions.

Unsupported mutating/runtime operations now fail closed in generated assembly. This is a safety improvement, not a completeness claim.

### 2. Do `fn`, `action`, and `lock` work together cleanly?

Yes for the local compiler boundary; still incomplete at the broader authorization/protocol-spec level.

- `fn` is a distinct pure helper category in AST, IR, codegen, and metadata.
- no-return `fn` calls have internal `Unit` type, produce no IR destination, and are accepted only as statement calls.
- resolved calls validate argument count and argument types before IR lowering.
- `action` is the main state-transition entrypoint and carries effect/scheduler/access metadata.
- `lock` is a distinct authorization entrypoint and now rejects non-bool return definitions.
- `action` and `lock` can call `fn`.
- `fn` can call only `fn`; attempts to call `action`, `lock`, `env::*` runtime builtins, or `type_hash()` Cell identity builtins are compile errors.
- `lock` cannot call `action` or another `lock`.

The remaining weakness is not parsing. It is semantic specification:

- how witness/signature domains are bound for `lock`
- how authorization helpers interact with stateful action validation

### 3. How much real DAG integration exists?

More than structural metadata, but not complete.

Real today:

- effect class metadata
- `touches_shared`
- operation-tagged `consume_set`, operation-tagged `read_refs`, operation-tagged `create_set`, and replacement-bound `mutate_set` with checked TypeHash/LockHash preservation, fixed-width preserved-field equality status, and scalar parameter/parameter-field/computed-local transition status
- per-action scheduler witness Borsh hex with operation-tagged Input/CellDep/Output access records; runtime-only witness/signature syscalls are not encoded as scheduler accesses
- `ckb_runtime_accesses` with operation/source/index/binding
- low-level `spora-exec` CellTx helpers that attach/discover/decode/admit scheduler witnesses, reject invalid operation/source classes or out-of-bounds transaction source indexes, and compare operation/source/index/binding_hash access sets against trusted summaries
- consensus MPE `BlockAccessSummary` consumption of admitted scheduler witnesses: scheduler-visible Input/CellDep/Output accesses are merged into concrete block summaries, and shared write/read or write/write overlaps serialize the execution DAG while shared read/read overlaps stay parallelizable
- strict consensus MPE `BlockAccessSummary` construction against trusted transaction-builder or compiled-metadata access multisets, with missing or mismatched summaries rejected before scheduler merge
- `fail_closed_runtime_features` for unsupported runtime paths that the generated assembly rejects
- entrypoint parameter ABI metadata
- CLI-accessible JSON through `cellc metadata`

Still missing:

- authenticated RPC or submission-surface support if external wallet submission must deliver trusted summaries into strict template policy rather than using the in-process mining API
- broader adversarial/property coverage before production trust
- formal derivation rules for every source-level effect and access path

### 4. Has CellScript completed what the design document specifies?

No.

Completed or materially real:

- compiler core
- RISC-V assembly
- pure RISC-V ELF
- optional CKB-VM runner for no-argument pure ELF
- restricted read-only CKB-runtime ELF for `read_ref<T>().scalar_field`
- restricted consumed-input field verification for fixed scalar fields
- restricted create-output field verification for fixed scalar fields
- restricted input-field-to-output-field equality verification for fixed `u64` aliases
- restricted left-associative `u64` add/sub output verification
- restricted created-output and schema-parameter TypeHash byte sources for output verification and fixed-byte comparisons
- simple local-constant and move/alias source propagation for prelude expressions
- package build/check/doc/fmt flows
- compiler-test discovery, expected-failure diagnostics, per-file target/policy directives, runtime metadata assertions, verifier-obligation assertions, runtime-required-obligation assertions, and entrypoint classification assertions through `cellc test`
- metadata sidecars and `cellc metadata`
- `cellc verify-artifact` for emitted artifact/metadata consistency checks
- partial IDE/LSP path
- action hover with lowering/runtime metadata
- lowering/runtime diagnostics for ELF-incompatible symbolic actions
- code actions for inspecting metadata or using asm while executable stateful lowering is incomplete
- partial CKB-style runtime ABI lowering
- typed schema layout metadata and restricted executable `u64` parameter/consume/read_ref field lowering
- no-return `Unit` helper calls, Unit-valued assertions, unreachable-statement rejection, tail-expression return lowering, typed empty arrays, `Vec.push` item propagation, and unknown-call return rejection

Not complete:

- full executable stateful runtime lowering
- full schema decoding from all loaded cell bytes and field invariant enforcement
- generalized conservation checks connecting consumed inputs to created outputs
- right-nested/arbitrary arithmetic state expressions
- full create expression/resource-handle executable semantics
- full resource/effect/lifecycle-transition soundness
- full witness/signature/domain binding story
- full DAG scheduler integration
- full custom runtime/property/fuzz/invariant testing
- full package registry lifecycle
- executable wasm backend, if it remains a claimed target

## Go / No-Go Judgment

For pure-compute scripts and compiler/tooling development: **Go**.

For serious production stateful protocol contracts: **No-Go** until the following are complete:

- generalized typed schema decoding from CKB-style loaded cell bytes
- executable `consume`, `create`, `transfer`, `claim`, `settle`, and `destroy` semantics
- static checks proving effect/resource declarations match lowered behavior
- witness/signature/domain binding rules with negative tests
- complete scheduler metadata enforcement in the real DAG/runtime path, including builder placement, trusted access-set comparison, and adversarial scheduling tests
- property/fuzz/adversarial transaction tests around invariants

The safe external claim remains:

> CellScript has a real CKB-VM-targeting compiler core and auditable metadata path, but it is not yet a complete production-grade stateful smart contract language.
